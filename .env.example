# Copy to .env to override. Defaults below match `npm run db:up`.
PGHOST=localhost
PGPORT=5433
PGUSER=postgres
PGPASSWORD=tigerlab
PGDATABASE=tigerlab

# Point these at a Tiger Cloud service to run the lessons there instead.
# PGHOST=<service>.tsdb.cloud.timescale.com
# PGPORT=xxxxx
# PGSSLMODE=require
